apply plugin: 'java'

dependencies {
    compile project(':core')
    compile 'com.yammer.metrics:metrics-core:2.2.0'
}


//TODO exclude kafka or indeed any provided dependencies
jar {
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}